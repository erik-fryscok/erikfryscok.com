{
  "name": "erikfryscok.com",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "check": "astro check && tsc --noEmit",
    "test": "npm run build && node --test tests/*.test.mjs",
    "build": "npm run check && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.2",
    "@tailwindcss/vite": "^4.0.0",
    "tailwindcss": "^4.0.0"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
