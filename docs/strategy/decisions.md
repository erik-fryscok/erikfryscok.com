# Decision Log

Decisions are recorded here when they shape direction or avoid revisiting the same question later.

| Date | Decision | Rationale | Status |
| --- | --- | --- | --- |
| 2026-07-20 | Position the personal presence at the intersection of engineering leadership and enterprise AI. | This is a differentiated fit for existing experience in AI, documentation, DevOps, cloud infrastructure, architecture, and team leadership. | Active |
| 2026-07-20 | Use `erikfryscok.com` as the personal-name domain. | A personal brand stays flexible while consulting, products, and career goals evolve. The domain is managed through Cloudflare and is the canonical production address. | Active |
| 2026-07-20 | Make the personal website the first concrete initiative. | It is a professional home base that can support consulting, writing, products, and future projects. | Active |
| 2026-07-20 | Keep the first site release narrow: Home, About, Writing, Projects, and Contact. | A credible live foundation matters more than an elaborate but empty site. | Active |
| 2026-07-20 | Use Astro with TypeScript and Tailwind CSS; target Cloudflare Pages for hosting. | The stack is fast, SEO-friendly, content-friendly, and matches existing cloud experience. | Chosen |
| 2026-07-20 | Delay newsletters, courses, product storefronts, search, RSS, and comments until the core site and useful content exist. | These are expansion paths, not launch requirements. | Active |
| 2026-07-20 | Create `erikfryscok.com` as a public GitHub repository owned by `erik-fryscok`. | Public visibility supports discoverability, credibility, and future collaboration around the personal website. | Chosen |
| 2026-07-20 | Initialize the repository with a README and the GitHub Node `.gitignore` template. | The README gives the project an immediate public entry point; the Node template matches the planned Astro/TypeScript toolchain. | Chosen |
| 2026-07-20 | Do not add a license at repository creation. | The repository will contain personal writing, branding, images, and potentially employer-informed ideas. Licensing the code can be revisited later with separate treatment for code and content. | Chosen |
| 2026-07-23 | Manage planning model-selection guidance via a Cline workspace rule (`.clinerules/`), scoped to direct creator APIs on pay-per-use terms for cost tracking; route by task reasoning load across provider families and tiers rather than pinned model versions. | This established a measurable BYOK baseline before the local fleet became viable. | Superseded |
| 2026-07-25 | Use a hamburger menu with `is:inline` script for mobile navigation in BaseLayout. | Minimal client-side interactivity avoids Astro client component hydration overhead; Tailwind responsive utilities handle show/hide; inline script toggles CSS classes and ARIA attributes. | Chosen |
| 2026-07-24 | Make validated local open-weight inference the coding baseline, with no paid inference required; route by local role alias and escalate after measured failure. | A private llama.cpp fleet now provides reproducible model, runtime, context, compatibility, and cost evidence while keeping raw machine state out of the public repository. Direct APIs remain optional comparison tools, not a project dependency. | Active |
| 2026-07-31 | Use SVG as primary favicon with PNG fallbacks (16×16, 32×32), Apple Touch Icon (180×180), and Web App Manifest for cross-platform coverage. Logo SVG in nav header replaces plain-text brand. Assets served from Astro's default `public/` directory. | SVG provides crisp rendering at any resolution; PNG fallbacks cover legacy browsers; Apple Touch Icon and manifest enable home-screen installation on iOS and Android. `public/` is Astro's convention for static assets, copied verbatim to `dist/` during build. | Chosen |
| 2026-08-01 | Split OpenCode agent configuration: identity (description, model, mode, prompt) lives in `.opencode/agents/*.md`; behavior (temperature, steps, permissions, task rules) lives in `opencode.jsonc`. Review acts as an orchestrator with three exclusive specialist subagents (code-review, security, documentation). Models are assigned per agent from the OpenCode Zen catalogue by cost tier, avoiding deprecated and free data-retaining models. | One source of truth per agent keeps prompts reviewable in markdown while guardrails stay centralized and auditable in jsonc. Orchestration keeps review quality high (Sonnet 5 specialists) while routine exploration and delegation run on sub-dollar models (GPT 5.4 Nano). Free Zen models were excluded because several retain data for training, conflicting with the repository's content boundaries. | Active |
| 2026-08-07 | Place Erik's portrait in the Home hero rather than the Contact page, using Astro's managed image pipeline. | The Home placement humanizes the first impression and balances the text-heavy hero; the Contact page remains focused on outreach methods. Astro-managed WebP output reduces image transfer size while retaining the source asset in Git. | Chosen |
| 2026-08-07 | Add a standalone `/now` page for current priorities while retaining `/writing` as the article archive. | A now page answers what Erik is focused on at this point in time; keeping it separate from Writing preserves clear purposes for personal updates and durable articles. | Chosen |
| 2026-08-09 | Generate a sitemap and production canonical URLs for the public launch. | The site now has enough public routes and a live canonical domain to benefit from explicit crawl discovery and duplicate-URL control; Astro generates the sitemap from the static route set. | Active |
| 2026-08-09 | Use the generic `erik-fryscok/skills` name for the Agent Skills collection. | The skills CLI and skills.sh foreground individual skill names while retaining `owner/repo` as source provenance, so a generic collection name supports focused skill discovery and future growth. Standalone MCP products or plugins can still receive separate repositories when appropriate. | Chosen |
