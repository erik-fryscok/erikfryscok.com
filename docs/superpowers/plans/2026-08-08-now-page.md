# Focused Now Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a public `/now` snapshot page covering Erik’s current professional and personal priorities while retaining `/writing` as the article archive.

**Architecture:** Add one static Astro page that consumes the existing `BaseLayout`; add one navigation link in the shared layout so desktop and mobile menus stay in sync. Update durable decision/changelog documentation alongside the user-visible route.

**Tech Stack:** Astro 7, TypeScript, Tailwind CSS 4, Markdown documentation.

## Global Constraints

- Keep `/writing` and its published article unchanged.
- Use semantic `h1`/`h2` headings and the existing narrow content layout.
- Do not add JavaScript, dependencies, images, or content-collection machinery.
- Keep copy public-safe and omit employer/client confidential information.

### Task 1: Add the `/now` page

**Files:**
- Create: `src/pages/now.astro`

- [ ] **Step 1: Create the page with the shared layout**

Use `BaseLayout` with title `Now — Erik Fryscok`, a `max-w-2xl mx-auto` wrapper, an `h1` of `Now`, a short explanation, and a visible `Last updated: August 7, 2026` line.

- [ ] **Step 2: Add the three focused sections**

Add `h2` sections titled `Building better engineering systems`, `Playing guitar again`, and `Riding new routes`, with concise paragraphs based on the approved public-safe content.

- [ ] **Step 3: Check the new page markup**

Run `rg -n "<h1|<h2|Last updated|BaseLayout" src/pages/now.astro` and confirm one h1, three h2 headings, the date, and the shared layout import are present.

### Task 2: Add the route to shared navigation

**Files:**
- Modify: `src/components/BaseLayout.astro`

- [ ] **Step 1: Add the Now link**

Insert `<li role="menuitem"><a href="/now" ...>Now</a></li>` between the About and Writing links, preserving the existing classes, IDs, inline menu script, and mobile behavior.

- [ ] **Step 2: Verify navigation references**

Run `rg -n 'href="/(about|now|writing)"' src/components/BaseLayout.astro` and confirm the order is About, Now, Writing.

### Task 3: Record durable project context

**Files:**
- Modify: `docs/strategy/decisions.md`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Add the decision-log entry**

Record that `/now` is a focused current-priorities snapshot and complements, rather than repurposes, `/writing`.

- [ ] **Step 2: Add the Unreleased changelog entry**

Under `Unreleased` → `Added`, record the new `/now` page and its navigation link.

- [ ] **Step 3: Validate Markdown formatting**

Run `git diff --check` and inspect the changed documentation sections for valid Markdown links and table formatting.

### Task 4: Verify the site

**Files:**
- Test: generated Astro output from `npm run check` and `npm run build`

- [ ] **Step 1: Run diagnostics**

Run `npm run check`; expect exit code 0 with no diagnostics.

- [ ] **Step 2: Run the production build**

Run `npm run build`; expect exit code 0 and a generated `/now/index.html` in `dist/`.

- [ ] **Step 3: Verify generated content and route**

Run `rg -n "Building better engineering systems|Playing guitar again|Riding new routes|/now" dist/now/index.html dist/*/*.html` and confirm the page content and navigation link are emitted.
